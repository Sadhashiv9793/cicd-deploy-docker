{
  "name": "cicd-deploy-docker",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/Sadhashiv9793/cicd-deploy-docker#readme",
  "bugs": {
    "url": "https://github.com/Sadhashiv9793/cicd-deploy-docker/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sadhashiv9793/cicd-deploy-docker.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/app.ts",
  "scripts": {
"start": "ts-node src/app.ts",
"deploy:stop-container": "docker stop test-server || true",
"deploy:remove-container": "docker container rm test-server || true",
"deploy:start-container": "docker run --name test-server -d -p 3000:3000 test-server",
"deploy:docker-build": "docker build . -t test-server",
"deploy": "npm run deploy:docker-build && npm run deploy:stop-container && npm run deploy:remove-container && npm run deploy:start-container"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
